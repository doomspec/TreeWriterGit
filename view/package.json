{
  "name": "treewriter-view",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "dev": "pnpm --parallel --recursive dev",
    "build": "pnpm --recursive build"
  },
  "pnpm": {
    "overrides": {
      "js-yaml": "^4.2.0",
      "tsx>esbuild": "^0.28.1"
    },
    "patchedDependencies": {
      "gray-matter@4.0.3": "patches/gray-matter@4.0.3.patch"
    }
  }
}
