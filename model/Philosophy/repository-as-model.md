# Repository as Model

The repository is the model.

Views are generated automatically from repository structure and metadata.

As coding agents become increasingly capable, generating specialized views becomes cheaper than designing rigid application-specific data models. Instead of building a custom application for each domain, information can be stored in a Git repository and the most appropriate interface can be generated dynamically.

