Import BibTeX from **Assets → References → Upload .bib**. Each entry becomes `notes/literature/{file}.md` with a `cite_key`.

Cite in drafts with `[@cite_key]` or `[@a2024; @b2020]`. Do **not** use `\cite{}` in `draft.md` — export uses Pandoc citation syntax.
