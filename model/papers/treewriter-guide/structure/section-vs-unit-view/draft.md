**Section view** stitches approved child unit drafts into a composed preview — use it to read the whole section and run fan-out AI dispatch. **Unit view** focuses on one paragraph for tight editing.

Open the **Document outline** panel (`⌘⇧O`) to jump between headings and linked children.
