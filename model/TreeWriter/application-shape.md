# Application Shape

The system consists of two top-level directories:

```text
/
+-- model/
`-- view/
```

The `model/` directory contains document content and structure. The `view/` directory contains the generated application that visualizes and interacts with the model.
